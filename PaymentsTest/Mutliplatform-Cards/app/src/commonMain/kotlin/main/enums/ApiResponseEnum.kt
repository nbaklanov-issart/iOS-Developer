package overcoming.model.enum

enum class ApiResponseEnum {
    NOT_SET,
    EXCEPTION,
    SUCCESS,
    CREATED,
    FORBIDDEN,
    EXPIRED,
    INTERNAL_SERVER_ERROR,
    TO_MANY_REQUESTS,
    TO_EARLY,
    FAILED_DEPENDENCY,
}